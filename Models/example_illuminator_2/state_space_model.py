class Model:
    def __init__(self, inputs, outputs, parameters, states):
        self.inputs = inputs
        self.outputs = outputs
        self.parameters = parameters
        self.states = states

        self.meta = 
    def step(self):
        
